export const environment = {
  production: true,
  // Reemplaza esta URL por la del backend desplegado en Render
  // (ver DEPLOY.md), antes de desplegar el frontend en Vercel.
  apiUrl: 'https://REEMPLAZAR-CON-TU-BACKEND.onrender.com/api/tasks',
};
